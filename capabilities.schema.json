{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Zarr Capability Manifest",
  "type": "object",
  "properties": {
    "version": {
      "type": "integer"
    },
    "imports": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "capabilities": {
      "type": "object",
      "properties": {},
      "additionalProperties": {
        "$ref": "#/$defs/Capability"
      }
    }
  },
  "required": [
    "version",
    "capabilities"
  ],
  "$defs": {
    "Capability": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string"
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "support": {
          "type": "boolean"
        }
      }
    }
  }
}
