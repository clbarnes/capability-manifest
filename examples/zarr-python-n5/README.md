# zarr-python-n5

Capability manifest which could be added to [zarr-python-n5](https://github.com/clbarnes/zarr-python-n5).

Note that the descriptions/ URLs are omitted and not all capabilities are supported.
