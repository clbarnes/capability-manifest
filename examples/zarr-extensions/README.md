# zarr-extensions

Capability manifests which could be added to zarr-extensions: <https://github.com/zarr-developers/zarr-extensions>

Note that the top-level manifest aggregates that of each of the extension types; each extension type's manifest aggregates that of each of the extensions.
